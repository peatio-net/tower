import * as React from 'react';

class OrderBooksScreen extends React.Component {
    public render() {
        return (
            <React.Fragment />
        );
    }
}

export const OrderBooks = OrderBooksScreen;
