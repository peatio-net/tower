import * as React from 'react';
import {
    UsersTableContainer,
} from '../';

class UserDirectoryScreen extends React.Component {
    public render() {
        return (
            <UsersTableContainer />
        );
    }
}


export const UserDirectory = UserDirectoryScreen;
