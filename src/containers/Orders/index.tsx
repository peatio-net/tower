import * as React from 'react';

class OrdersScreen extends React.Component {
    public render() {
        return (
            <React.Fragment />
        );
    }
}

export const Orders = OrdersScreen;
